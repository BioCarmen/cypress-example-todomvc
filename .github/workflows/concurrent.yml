name: concurrent
on:
  workflow_dispatch:
    inputs:
      core-api:
        type: boolean
        description: ore-api

      bio-pusher:
        type: boolean
        description: bio-pusher-services

jobs:
  check:
    name: Check if job should run
    runs-on: ubuntu-20.04
    steps:
      - name: see
        uses: actions/github-script@v6
        with:
          script: |
            const inputs = context.payload.inputs
            const projects = Object.entries(inputs).filter(input=>input[1]==='true').map(input=>input[0])
            console.log(projects)
